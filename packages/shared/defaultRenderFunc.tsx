import React from 'react';
const renderContainer = (props?: any) => {
  return <div />;
};

const renderPanel = (props?: any) => {
  return <div />;
};

const renderContent = (props?: any) => {
  return <div />;
};

export { renderContainer, renderPanel, renderContent };
